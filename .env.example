# Qwen TTS Configuration
# Copy this file to .env to customize settings

# Model Size Selection
# Options: 0.6B (faster, smaller, good for CPU) or 1.7B (better quality, needs more resources)
MODEL_SIZE=1.7B

# Default Speed Setting (can be overridden per request)
# Range: 0.5 to 2.0, where 1.0 is normal speed
# Higher values = faster speech generation AND faster inference time
DEFAULT_SPEED=2.0
